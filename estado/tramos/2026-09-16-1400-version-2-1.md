# Tramo 2026-09-16 — Versión 2.1 estable

- **Inicio:** 2026-09-16 (tras el mandato del PO; hora aproximada en el nombre del archivo)
- **Fin:** 2026-09-16 15:56 — **Duración:** unas 2 h
- **Mensajes:** #026–#034
- **Mandato del PO:** «Mejora la herramienta para que sea rigurosa y bonita como tú quieras. Debo
  poder activar los incrementales y configurarlos desde ella sin HestiaCP, pero al mismo tiempo
  que me diga qué hizo. Es hora de potenciarla. https://hestiacp.com/docs/introduction/getting-started
  https://deepwiki.com/hestiacp/hestiacp. Y dejar una versión estable.»

## Antes de empezar (mismo día)

- Prueba de restauración del PO con `adoptar --como` sobre el propio servidor de pruebas:
  recuperó lo que quería; encontró un aviso falso de fallo (bug en el roadmap). Cerrada.
- Queja recurrente del PO: la interfaz web pesada y poco intuitiva (roadmap, memoria).
- Un nuevo cierre de los editores del PO: sin daños (árbol en `master`, rama
  `fix/limpieza-al-salir` intacta, sin temporales). La sesión del coder es nueva.

## Rondas

| # | Qué | Resultado | Commits |
| --- | --- | --- | --- |
| #026→#027 | Saludo tras el segundo cierre de los editores | el coder conserva el contexto | — |
| #028→#029 | Fase 1: documentación a `master`; `release/2.1`; terminar `fix/limpieza-al-salir` y fusionarla en `release/2.1` | detenido en C1 (como pedía la instrucción): A, B y C0 hechos | A: `e95697d` `62f1f0c` + fusión; `release/2.1` creada; C0 fusión de `release/2.1` en la rama |
| #030→#031 | Seguir la fase 1 desde C1 | C1–C7 hechos; D bloqueado: hallazgos graves en las revisiones; sin fusionar | `d4497b4` `a635e00` `3e70990` `f44c00f` `5db1343` `107ace0` `6fc8650` |
| #032→(#034) | Fase 1, segunda vuelta: señales, aritmética con datos remotos, limpieza de secretos, menciones a agentes, validaciones de la CLI | cortada a petición del PO (cierre del día): A, S1 y S2 hechos a las 15:54 | A: fusión `bc125af` en `master`; `bf7fe6f` (release/2.1 a la rama); `9a8edac` `b85f8b0` |
| #033→#034 | Cierre del día: terminar y commitear el arreglo en curso (S3) y parar | completado; `verificar.sh` falla solo en «menciones» (se quita en S4) | `04dcd54` |

## Cierre

- **Fin:** 2026-09-16, a petición del PO. **Mensajes:** #026–#034.
- **Resumen:** se decidió el camino a la versión 2.1 estable (ADR 0013) y se diseñaron, con la
  fuente de HestiaCP 1.10.4 verificada, el informe de lo hecho (0014), la interfaz por tareas (0015)
  y los incrementales de punta a punta (0016). La fase 1 avanzó en `fix/limpieza-al-salir`: T2
  corregido en su raíz (lo que además arregla T22 y T23), existencia de `restic.conf` con centinela,
  reintento de limpiezas que fallan, validación de `--snapshot`, conteos sin doble cero, aviso claro
  de `adoptar --como`; en la segunda vuelta, la limpieza protege a sus hijos de Ctrl-C, no se hace
  aritmética con datos del servidor y la limpieza de secretos remotos avisa si falla. Dos revisiones
  por vuelta; el arquitecto revirtió su propia decisión sobre señales tras la demostración de
  `security-auditor`. Pendiente: S4–S8 y la revisión final de la fase 1.

Decisión revertida en #032: la de #028 sobre las señales (C4). El arquitecto dictó un manejador
no vacío creyendo que protegía la limpieza; `security-auditor` demostró que Ctrl-C llega a todo
el grupo de procesos y mata al `ssh` hijo. Se vuelve a `trap ''` y se acota el cuelgue con
keepalive de `ssh`. Depositados como Propuesta los ADR 0014, 0015 y 0016. La comprobación de
menciones a IA pasa a detectar nombres de agentes (no había ninguno en commits posteriores a la
adopción).

Hallazgo de #029 (C1): `bc_adoptar_registrar` (`lib/adoptar.sh:1451`) le pasa un heredoc a
`bc_ssh_sudo`; la comprobación de `id -u` se lo comía (T2), así que la detección de bases
huérfanas nunca vio ninguna. Al arreglar T2 en su raíz empieza a funcionar de verdad.

Verificado antes de dictar #028: `bc_ssh_sudo` tampoco cierra stdin en su comprobación de
`id -u` (raíz de T2); el patrón `grep -c … || echo 0` también está en `lib/archive.sh:162` y
`lib/doctor.sh:100,112`; la CLI no valida `--snapshot` (`bin/backupctl:511`), la web sí
(`web/server.py:82-84`).

## Encontrado y decidido

- ADR 0013: todo el programa en `release/2.1`; prueba única del PO al final; etiqueta `v2.1.0`.
- Interpretación de «sin HestiaCP» (decisión del arquitecto, revertible por el PO): sin entrar al
  panel ni a su consola, pero usando por debajo el mecanismo de Restic de HestiaCP, para que la
  pantalla del panel que lista y restaura esas copias siga funcionando.
- Investigación lanzada: ciclo completo de incrementales en la fuente de HestiaCP 1.10.4
  (`hestia-verifier`) y lo que la herramienta ya hace con Restic y cómo está la web (explorador).

## Falló por el camino

## Espera al PO

Ver `estado/AHORA.md`.

## Resumen

Ver «Cierre».
